spring:
  application:
    name: notice
  datasource:
    url: jdbc:postgresql://localhost:5432/business
    username: postgres
    password: 1004
    driver-class: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081